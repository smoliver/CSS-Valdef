{
  "name": "css-valdef",
  "version": "1.0.0",
  "description": "A package for testing CSS values against CSS Value Definition Syntax.  (CSS Value Definition Syntax)[https://developer.mozilla.org/en-US/docs/Web/CSS/Value_definition_syntax] is used for defining the set of valid values for a CSS property or function.",
  "main": "./distribution/index.js",
  "scripts": {
    "build": "babel source --out-dir distribution",
    "watch": "babel source --watch --out-dir distribution",
    "test": "npm run build && mocha --require babel-core/register",
    "start": "npm run build && node ./distribution/index"
  },
  "keywords": [
    "CSS",
    "Syntax",
    "Style"
  ],
  "author": "Sam Grad Oliver",
  "license": "ISC",
  "devDependencies": {
    "assert": "^1.4.1",
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "source-map-support": "^0.4.9"
  }
}
